export type Point = {
    x: number;
    y: number;
};

export type PointColor = [number, number, number];

export type HoverElement = {
    x: number;
    y: number;
    width: number;
    height: number;
};
