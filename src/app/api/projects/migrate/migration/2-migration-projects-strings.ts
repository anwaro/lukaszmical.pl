// import {SupabaseProject} from '@/services/supabase/SupabaseProject';
// import {SupabaseProjectString} from '@/services/supabase/SupabaseProjectString';

export async function migrateProjectsStrings() {
    // const projectClient = new SupabaseProject();
    // const client = new SupabaseProjectString();
    //
    // const projects = await projectClient.getProjects(false);
    // return client.createProjectStrings(
    //     projects.flatMap((project) => [
    //         {
    //             projectId: project.id,
    //             type: 'name',
    //             locale: 'pl',
    //             value: project.name,
    //         },
    //         {
    //             projectId: project.id,
    //             type: 'description',
    //             locale: 'pl',
    //             value: project.description,
    //         },
    //         {
    //             projectId: project.id,
    //             type: 'content',
    //             locale: 'pl',
    //             value: project.content,
    //         },
    //         {
    //             projectId: project.id,
    //             type: 'name',
    //             locale: 'en',
    //             value: project.name,
    //         },
    //         {
    //             projectId: project.id,
    //             type: 'description',
    //             locale: 'en',
    //             value: project.description,
    //         },
    //         {
    //             projectId: project.id,
    //             type: 'content',
    //             locale: 'en',
    //             value: project.content,
    //         },
    //     ]),
    // );
}
