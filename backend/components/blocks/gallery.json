{
  "collectionName": "components_blocks_galleries",
  "info": {
    "name": "gallery",
    "icon": "photo-video",
    "description": ""
  },
  "options": {},
  "attributes": {
    "photos": {
      "collection": "file",
      "via": "related",
      "allowedTypes": [
        "images"
      ],
      "plugin": "upload",
      "required": true,
      "pluginOptions": {}
    },
    "view": {
      "type": "enumeration",
      "enum": [
        "single",
        "slider",
        "list",
        "grid"
      ],
      "default": "single",
      "required": true
    }
  }
}
